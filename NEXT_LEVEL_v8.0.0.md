# Vision AI v8.0.0 — Next Level

## New
1. **PWA** — installable (manifest + icons + service worker shell cache)
2. **Model status pills** — live `/api/llm/health` in header (which providers look configured)
3. **vision-system** retained as single design system
4. Utility grid/buttons from v7.3

## Still requires
- Valid API keys on Railway for cascade
- Redeploy + hard refresh

## Not a full React rewrite
Next level = product reliability + installability, not a framework swap.
